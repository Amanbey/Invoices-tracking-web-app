# Backend

## Environment
Create a `.env` file with:

```
PORT=5000
MONGO_URI=mongodb://localhost:27017/invoice_tracking
JWT_SECRET=change_this_secret
JWT_EXPIRES_IN=1d
```

## Run

```
npm install
npm run dev
```
